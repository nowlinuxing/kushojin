require "spec_helper"

RSpec.describe Kushojin do
  it "has a version number" do
    expect(Kushojin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
